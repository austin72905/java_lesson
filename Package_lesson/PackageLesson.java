package Package_lesson;

public class PackageLesson {
    
}

/* 
    package 

    1. 為了更好管理class
    2. 使用 package  聲明class 所屬的包 (代表這個class 是屬於哪個包)， 聲明在文件首行
    3. 每個"." 都代表一層文件目錄
    4. 同個 package 不可命名同名的class ， 不同包的 可以命名同名的


    import 是 導入包

    1. xxx.* 代表導入package 裡 所有的class
    2. 核心包 java.lang 下 就不用導入
    3. 當前包下的class 也不用引入
    4. 如果使用兩個不同包，同名的class ， 只能用完整包名 ， 無法import了
    5. 使用 xxx.* ，包裡的 子包 還是需要import
    6. import static 使用包裡靜態的結構: 屬性 or 方法  ex: import static java.lang.System.*
*/
